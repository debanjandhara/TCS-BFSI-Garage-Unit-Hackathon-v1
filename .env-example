# OpenAI API Configuration
# Get your API key from https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Model Configuration
LLM_MODEL=gpt-4o-mini
EMBEDDING_MODEL=text-embedding-3-small

# File Paths
DATA_PATH=data/
VECTORSTORE_PATH=vectorstore/db_faiss
LOGS_PATH=logs/

# Uncomment for debugging
# PYTHONPATH=.
# DEBUG=True

# Note: This is an example file. Copy it to .env and replace the values with your actual configuration.
# The .env file is already in .gitignore to keep your sensitive information private. 